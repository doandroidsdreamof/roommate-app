import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  container: {
    paddingVertical: 8,
  },
  column: {
    flexDirection: 'column',
    alignItems: 'center',
    gap: 8,
  },
  button: {
    flex: 1,
  },
});
