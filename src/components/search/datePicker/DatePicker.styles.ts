import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  button: {
    flex: 1,
    width: '100%',
  },
  column: {
    alignItems: 'center',
    flexDirection: 'column',
    gap: 8,
  },
  container: {
    paddingVertical: 8,
  },
});
