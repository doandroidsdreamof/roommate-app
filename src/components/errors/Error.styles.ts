import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  actions: {
    gap: 12,
    maxWidth: 300,
    width: '100%',
  },
  button: {
    margin: 'auto',
    width: '70%',
  },
  container: {
    alignItems: 'center',
    flex: 1,
    justifyContent: 'center',
    padding: 24,
  },
  message: {
    marginBottom: 24,
    opacity: 0.7,
    textAlign: 'center',
  },
  title: {
    marginBottom: 8,
    marginTop: 24,
    textAlign: 'center',
  },
});
