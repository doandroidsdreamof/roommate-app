import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  container: {
    alignItems: 'center',
    padding: 20,
  },
  message: {
    marginBottom: 8,
    textAlign: 'center',
  },
  resetText: {
    textAlign: 'center',
  },
});