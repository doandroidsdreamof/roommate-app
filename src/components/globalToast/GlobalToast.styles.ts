import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  container: {
    bottom: 0,
    left: 0,
    position: 'absolute',
    right: 0,
  },
  toastWrapper: {
    left: 0,
    position: 'absolute',
    right: 0,
  },
});
